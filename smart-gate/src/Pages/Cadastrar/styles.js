import styled from "styled-components";
import { colors, screenWidth } from "../../Constants/constants";

export const CadastroContainer = styled.View`
    flex: 1;
    align-items: center;
    justify-content: flex-start;
    margin-top: ${screenWidth * 0.04}px;
    gap: ${screenWidth * 0.08}px;
`;